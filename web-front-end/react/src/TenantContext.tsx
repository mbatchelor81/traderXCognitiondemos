/**
 * TenantContext is no longer needed in single-tenant mode.
 * Tenant isolation is enforced at the infrastructure level.
 * This file is kept as an empty placeholder to avoid breaking
 * any residual imports during the migration.
 */
export {};
