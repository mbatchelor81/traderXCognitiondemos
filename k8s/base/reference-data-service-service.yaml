# Generated as part of the TraderX Kubernetes base. Tenant overlays patch
# tenant-specific values (namespace, TENANT_ID, config).
apiVersion: v1
kind: Service
metadata:
  name: reference-data-service
  labels:
    app: reference-data-service
    app.kubernetes.io/name: reference-data-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: traderx
spec:
  type: ClusterIP
  selector:
    app: reference-data-service
  ports:
    - name: http
      port: 8004
      targetPort: 8004
      protocol: TCP
