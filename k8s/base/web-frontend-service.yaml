apiVersion: v1
kind: Service
metadata:
  name: web-frontend
  namespace: traderx
  labels:
    app: web-frontend
spec:
  type: ClusterIP
  selector:
    app: web-frontend
  ports:
    - port: 18094
      targetPort: 18094
      protocol: TCP
