apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: traderx
  labels:
    app: users-service
spec:
  type: ClusterIP
  selector:
    app: users-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
