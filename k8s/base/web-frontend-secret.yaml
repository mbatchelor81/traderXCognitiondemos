# Generated as part of the TraderX Kubernetes base. Tenant overlays patch
# tenant-specific values (namespace, TENANT_ID, config).
# Values are intentionally empty. Real values are injected at deploy time
# (External Secrets Operator / AWS Secrets Manager). Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: web-frontend-secret
  labels:
    app: web-frontend
    app.kubernetes.io/name: web-frontend
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: traderx
type: Opaque
stringData:
  DATABASE_URL: ""
